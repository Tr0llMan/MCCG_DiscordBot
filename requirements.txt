discord.py>=2.3,<3
aiomysql>=0.2
python-dotenv>=1.0
# TOML parsing for rolemap.toml. Python 3.11+ has tomllib in the stdlib (nothing to install);
# older Pythons use the tomli backport, which has the identical API. tomllib is NOT a pip
# package — do not add it here.
tomli>=2.0 ; python_version < "3.11"